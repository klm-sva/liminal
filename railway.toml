[build]
builder = "nixpacks"
buildCommand = "npm install --omit=dev"

[variables]
NODE_VERSION = "20"

[deploy]
startCommand = "node pipeline/worker.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
