[build]
builder = "nixpacks"
buildCommand = "npm install --omit=dev && npm run build:worker"


[deploy]
startCommand = "node pipeline/worker.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
